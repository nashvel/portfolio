Options -MultiViews
# RewriteEngine On

# RewriteBase /PorfolioPHP/Portfolio/

# Rule: Internally rewrite clean URLs to .php files (for Apache)
# If the request is not for an existing file or directory
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# And if the request + .php is an existing file
# RewriteCond %{REQUEST_FILENAME}.php -f
# RewriteRule ^(.*)$ $1.php [L]
